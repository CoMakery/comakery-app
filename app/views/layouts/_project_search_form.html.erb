<%= search_form_for policy_scope(Project).ransack, url: projects_path, method: :get do |f| %>
  <%= f.search_field :title_or_description_or_token_name_or_mission_name_cont, placeholder: 'Search all projects' %>
<% end %>