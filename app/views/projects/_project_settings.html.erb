<div class="project_settings">
  <%= react_component(
    'layouts/ProjectSetupHeader',
    project_for_header: @project.header_props,
    mission_for_header: @whitelabel_mission ? nil : @project&.mission&.decorate&.header_props,
    owner: policy(@project).edit?,
    observer: policy(@project).project_observer?,
    interested: policy(@project).project_interested?,
    expanded: false,
    current: project_page
  ) %>

  <div class="project_settings__content">
    <%= yield %>
  </div>
</div>
